-- SQL Schema for Tersoh Project
-- Create tables here. Example:

-- CREATE TABLE users (
--     id SERIAL PRIMARY KEY,
--     username VARCHAR(50) UNIQUE NOT NULL,
--     password_hash TEXT NOT NULL,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Add other tables: transactions, messages, KYC, rates, posts, etc.
